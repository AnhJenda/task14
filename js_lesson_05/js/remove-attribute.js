var firstItem = document.getElementById('one'); // get first item

if(firstItem.hasAttribute('class')){ // if it has class attribute

    firstItem.removeAttribute('class'); // remove class attribute
}